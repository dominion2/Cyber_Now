# 🛡️ Master Analysis: Stage 3 Payload Encryption & Structure
**Target File:** `nW0eNf35ZjkI6w` (2.1 MB)
**Associated Components:** `UKqqACLUALIsaSR` (Engine), `oXAAsaYOQC188UF` (Config)
**Classification:** Encrypted Core Payload (Ghostpulse/IDAT Variant)

---

## 1. Executive Summary
The file `nW0eNf35ZjkI6w` represents the final stage of the infection chain. It contains the primary malicious logic (e.g., Infostealer or RAT) but is stored in a highly obfuscated "Ghostpulse" container. It is mathematically locked to the Orchestrator's execution logic and cannot be decrypted using simple static XOR methods.

## 2. Structural Reconstruction

### 2.1 Masquerade Header
Initial forensic scans revealed that the file uses a **Decoy Header** to bypass simple file-type triage.
* **Offset 0x00:** Contains keyboard-mashed ASCII (`ttcsseieesnnwrrl`, `meletncnekattnoc`).
* **Purpose:** To trick basic AV scanners into classifying the file as a "log" or "text" data blob.

### 2.2 The IDAT Synchronization Point
The Loader (`UKqqACLUALIsaSR`) programmatically scans for a specific marker to locate the payload.
* **Anchor Hex:** `C6 A5 79 EA`
* **Discovery Offset:** **`0x4056`**
* **Significance:** This marks the boundary where "garbage" padding ends and the refined encrypted cargo begins.

## 3. Cryptographic Barrier Analysis

### 3.1 Dynamic Keying Discovery
Manual XOR tests using the Stage 2 key (`77UJ`) failed to produce any recognizable PE headers or ASCII. 
* **Forensic Verdict:** The payload uses **Session-Specific Keying**.
* **The Seed:** The first 4 bytes following the marker (**`0x405a`**) are `78 83 76 8a`. These bytes serve as the initialization vector for the SIMD engine.

### 3.2 Vectorized Decryption Mechanism
As identified in the Loader's SSE routine (`0x180003920`), the decryption uses a **Rolling XOR** combined with bitwise mutations:
1.  **Block Size:** 128-bit (16 bytes processed per CPU cycle).
2.  **Logic:** After each block, the key is modified (likely rotated) before the next XOR.
3.  **Result:** Standard static analysis tools cannot "guess" the key because it is constantly changing throughout the 2.1 MB stream.

## 4. Encrypted Metadata Table
A deep ASCII scan (`rabin2 -zz`) located a massive jumbled string table at **`0x37e4`**.
* **Content:** This block contains jumbled English characters (e.g., `twezstnfottsamu`).
* **Role:** In modern stealers (like LummaC2), this table stores C2 URLs, API names, and mutexes in a single obfuscated blob to prevent automated YARA matching.

---

## 🛠️ Forensic Reproduction Steps (Radare2)

### Locate the Synchronization Point:
```bash
r2 -q -c "/x C6A579EA" ./nW0eNf35ZjkI6w
```

### Inspect the Encryption Seed:
```bash
# View the 16 bytes immediately following the marker
r2 -q -n -c "s 0x405a; px 16" ./nW0eNf35ZjkI6w
```

### Identify the String Table:
```bash
rabin2 -zz ./nW0eNf35ZjkI6w | grep -i "0x000037e4"
```

---

## 🏁 Current Campaign Status
1. **✅ Stage 1 (Loader):** Engine fully mapped.
2. **✅ Stage 2 (Config):** Decrypted (Key: 77UJ).
3. **✅ Stage 3 (Payload):** Structure identified; Marker located at `0x4056`.

**Next Investigative Action:** Perform a Process Memory Dump after the Loader executes the SIMD routine to capture the Stage 3 payload in its unencrypted (MZ) state.

---
*Generated by Gemini Forensic Lab*
